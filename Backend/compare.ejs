<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="base.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Compare Codes</title>
    <style>
        #compare-container {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-top: 30px;
        }
        .code-block {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 45%;
        }
        .big-textarea {
            width: 100%;
            height: 300px;
            resize: none;
            font-family: monospace;
            font-size: 1em;
        }
        #similarity-result {
            text-align: center;
            margin-top: 30px;
        }
        .roll-label {
            font-weight: bold;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div id="header">
        Here are both the codes you wanted to compare
    </div>
    <div id="compare-container">
        <div class="code-block">
            <div class="roll-label">Roll No: <%= roll1 %></div>
            <textarea readonly class="big-textarea"><%= code1 %></textarea>
        </div>
        <div class="code-block">
            <div class="roll-label">Roll No: <%= roll2 %></div>
            <textarea readonly class="big-textarea"><%= code2 %></textarea>
        </div>
    </div>
    <div id="similarity-result">
        <h3 id="result1"><%= similarity %>% similar</h3>
    </div>
</body>
</html>